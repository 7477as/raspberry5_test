# 播放器启动后，会弹出一个窗口显示视频，并伴随声音输出。
# 在终端输入 p 回车，视频暂停。
# 在终端输入 s 15 回车，你会看到视频窗口瞬间刷新到了第 15 秒的关键帧画面，并且保持暂停状态不动。这就是未来绑定到 UI 进度条滑动事件上的核心逻辑。
# 输入 r 回车，视频从 15 秒处无缝继续播放。

# 使用 FFmpeg 重新编码测试视频的示例命令，修复I帧太稀疏问题
ffmpeg -i input.mp4 -c:v libx264 -g 30 -keyint_min 30 -flags +cgop -c:a copy output_fixed.mp4