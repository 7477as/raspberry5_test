---
description: 通用仓库规范 - 适用于所有文件的目录、命名、Git、文件头等约定。每次生成代码前必读。
globs: ["**/*"]
alwaysApply: true
---

# 通用代码生成规范（适用于整个仓库）

> 这是 Cursor 在生成 / 修改代码时必须遵守的通用规则。具体语言细节见同目录其它 `.mdc` 文件。

## 1. 仓库结构

仓库根只能有这些顶层目录：

- `cpp/`     —— C++ 示例工程（详见 `20-cpp.mdc`）
- `c/`       —— C 示例工程（详见 `10-c.mdc`）
- `python/`  —— Python 示例脚本 / 包（详见 `30-python.mdc`）
- `misc/`    —— 第三方二进制、配置、杂项
- `docs/`    —— 可选的额外文档
- `.cursor/` —— Cursor 编辑器配置 / rules

**禁止**：
- 在仓库根直接放散落的 `.c` / `.cpp` / `.py` / `.sh` 文件
- 新建顶层目录（除非先在 PR 中说明）
- 把构建产物 / 模型权重提交进 git（见根 `.gitignore`）

## 2. 功能组织

以"功能 / 场景"为单位组织目录：

```
cpp/<topic>/<feature>/       # 例: cpp/video/video_play_gstreamer/
c/<topic>/<feature>/         # 例: c/gpio/led_blink/
python/<topic>/<feature>/    # 例: python/yolo/yolo8_detection_default/
```

**每个功能子目录必须有 `README.md`**（详见 `40-readme.mdc`），禁止把多个互不相关的小示例塞进同一个目录。

## 3. 命名规范（速查）

| 类型 | 风格 | 示例 |
| :--- | :--- | :--- |
| 目录（功能） | `snake_case` | `video_play_gstreamer` |
| 文件（C++） | `snake_case` + 扩展 | `frame_buffer.cpp` / `frame_buffer.h` |
| 文件（C） | `snake_case` + 扩展 | `frame_buffer.c` / `frame_buffer.h` |
| 类 | `PascalCase` | `FrameBuffer` |
| 函数 / 变量 | `snake_case` | `open_camera()` / `frame_count` |
| 常量 / 宏 | `UPPER_SNAKE` | `BUFFER_FRAMES` |
| Python 模块 | `snake_case` | `ai_chat` |
| Python 类 | `PascalCase` | `AsrEngine` |
| Shell 脚本 | `snake_case.sh` | `build_run.sh` |

## 4. Git 工作流（生成 commit 信息时遵守）

提交信息必须遵循 **Conventional Commits**：

```
<type>(<scope>): <subject>

<body>

<footer>
```

- `type` ∈ `feat | fix | docs | style | refactor | perf | test | chore | build | ci`
- `scope` 用目录名（`cpp/video`、`python/yolo` 等）
- `subject` ≤ 72 字符，英文祈使句，首字母小写，末尾无句号

分支命名：
- `feat/<scope>-<short-desc>`：`feat/cpp-add-sdl-player`
- `fix/<short-desc>`：`fix/build-run-pkg-check`
- `docs/<short-desc>`：`docs/readme-camera-section`

## 5. 文件编码与换行

- **UTF-8 无 BOM**
- **LF 换行**
- 缩进：Shell/Python 用 4 空格；C++ 用 4 空格；Markdown 不强制
- 文件末尾留一个空行

## 6. Cursor 行为约束

1. **不要直接修改 `README.md`、`CONTRIBUTING.md` 等根级说明文档**，除非用户明确要求。
2. **不要删除或覆盖用户已有文件**——优先在原文件追加 / 修改；删除前必须确认。
3. **新增文件必须先在脑内确认目标目录存在**，不存在则先 `mkdir -p`。
4. **生成任何脚本 / 配置后，给出一句 `chmod +x` 提醒**（如果是可执行文件）。
5. **生成代码前先 Read 相关模板**（如 `cpp/_template/`、`python/_template/`），风格与模板保持一致。
6. **不要在回复中夹带无关的重构**——一个任务只做一件事。
7. **不确定时优先问问题**（使用 AskQuestion），不要凭猜测写代码。

## 7. 禁止提交的内容

Cursor 生成 / 建议文件时，**主动跳过**这些类型：

- `build/`、`*.o`、`*.so`、`*.a`、`*.exe`、可执行二进制
- `*.pt` / `*.onnx` / `*.hef` / `*.tflite` / `*.engine` / `*.safetensors`（模型权重）
- `.env`、`*.local`、`secrets.*`（凭据）
- `__pycache__/`、`.venv/`、`*.pyc`
- `.vscode/`、`.idea/`、IDE 配置

如果用户明确要求"生成示例模型下载链接 / 脚本"，给出 URL 或 `download_assets.sh`，**不要内联大文件**。

## 8. 输出风格

- 用中文回复（与用户当前对话语言一致）。
- 代码块使用对应语言标识符：`cpp` / `python` / `bash` / `cmake` / `text`。
- 引用仓库已有代码时使用 `startLine:endLine:path` 格式。
- 不要解释你已经做的显而易见的事；只解释**为什么**这么做。
